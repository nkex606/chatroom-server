apiVersion: v1
kind: Service
metadata:
  name: {{ include "chatroom-server.fullname" . }}
  labels:
    {{- include "chatroom-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chatroom-server.selectorLabels" . | nindent 4 }}
